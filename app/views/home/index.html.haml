%h3 Home

- if user_signed_in?
  %p
    Your passport code is
    = @user.passport_code unless @user.nil?
  %p
    Your current state is
    = @user.marina_state unless @user.nil?
  %p
    Your current marina is
    = link_to( @marina.name, marina_path(@marina)) unless @marina.nil?

  - if current_user.has_role? :admin
    MANAGER - INVITE USER
    = link_to( "invite user", new_user_invitation_path )
    = link_to( "edit user", user_invitation_path)

%h5 Welcome to the TEM Passport Application