%h3
  = t('marinas.management.active_managers')

.span8
%table.table.table-condensed
  %thead
    %tr
      %th= t('marinas.management.username')
      %th= t('marinas.management.email')
      %th= t('marinas.management.registered_date')
      %th= t('marinas.management.role')
      %th
  %tbody
    - @marina.active_managers.each do |user|
      %tr
        %td= link_to user.name, user
        %td= user.email
        %td= user.created_at.to_date
        %td= user.roles.first.name.titleize unless user.roles.first.nil?
        %td= link_to t('marinas.management.expire_manager'), expire_manager_users_path(user, :marina => @marina)
