%h3 Expired Managers


.span8
%table.table.table-condensed
  %thead
    %tr
      %th Username
      %th Email
      %th Registered
      %th Role
      %th
  %tbody
    - @marina.expired_managers.each do |user|
      %tr
        %td= link_to user.name, user
        %td= user.email
        %td= user.created_at.to_date
        %td= user.roles.first.name.titleize unless user.roles.first.nil?
        %td= link_to 'Revalidate Manager', revalidate_manager_users_path(user, :marina => @marina)
        %td= link_to 'Remove Manager', remove_expired_manager_users_path(user, :marina => @marina)
